# --------------------------------------------------
# Root Git Ignore for MERN Project Management System
# --------------------------------------------------
# Node dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Environment variables (never commit secrets)
.env
.env.*
!.env.example
backend/.env
frontend/.env

# Build outputs / artifacts
build/
dist/
frontend/build/
backend/build/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
lerna-debug.log*

# Coverage / test output
coverage/
backend/coverage/
frontend/coverage/

# System files
.DS_Store
Thumbs.db

# IDE/editor settings
.vscode/
.idea/
*.swp
*.swo

# Local SSL / keys / certs
*.pem
*.key
*.crt

# Optional: ignore the assignment PDF if not needed in repo
MERN-ASSIGNMENT-PRACTICE_SEPT2025 (1).pdf

# Temporary files
*.temp
*.tmp

# TypeScript build (if added later)
*.tsbuildinfo

# Lock files (KEEP these – they ensure deterministic installs)
# DO NOT remove package-lock.json entries below; just ensuring not ignored
!package-lock.json
!backend/package-lock.json
!frontend/package-lock.json

# Archive of historical docs can stay (remove if you prefer a cleaner repo)
# docs/archive/*

# End of .gitignore
